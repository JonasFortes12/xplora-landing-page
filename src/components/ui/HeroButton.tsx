import {Button} from "@heroui/button";

export default function HeroButton() {
  return <Button className="bg-blue-600 rounded-sm w-40 text-white cursor-pointer hover:scale-105 transition-all hover:bg-blue-700">Começar</Button>;
}
